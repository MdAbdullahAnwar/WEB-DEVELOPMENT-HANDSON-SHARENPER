Deliverable:


Inside the Button component return an html button element.

Render the children of Button component in between the opening and closing tags of the button element.

Correctly, apply the styles in Button.css to the Button component.

Import Button component in the AddUser component and replace the button element that you have inside the form with the Button component.

The text in between the opening and closing tags of the custom Button component will still be "Add User".

Give this custom Button a "type" attribute with value as "submit".

Now, inside Button component make use of props to set the type of button element there, and also make "Add User" the text inside the button element there.